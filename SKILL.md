---
name: political-meeting
description: 政治学习会议材料整理。收到文件名含"第X周""政治学习""学习内容""签到表""会议照片"的附件，或老师要求"把这两周的学习材料归入总文档""第X周还差什么""总表进度"时触发。严禁read_file读docx/doc，用insert_material.py和check_status.py脚本。
category: 工具
invocable: both
argument-hint: "<第X周> <材料类型> [文件路径]"
---

# 政治学习会议材料整理

## 🛑 禁止

**禁止 read_file 读 .docx / .doc。禁止 glob 搜 .docx。禁止自己写 python -c 分析文件。**

**禁止绕过脚本自己拼 OfficeCLI / PowerShell / Python 临时脚本修改 Word。** 如果 `insert_material.py` 或 `check_status.py` 报错，只能修正参数后重跑脚本，或把错误原文反馈给老师；不要手写 `officecli add/set`，不要用 `--index -1`，不要把学习材料正文直接拼进命令行。

**禁止把 docx 当 zip 解压分析。** 不要复制总文档到 `temp_analysis.docx`，不要改名为 `.zip`，不要读取 `word/document.xml`，不要调用 `read_file` 读取任何解压出的 XML。总文档很大，读取 XML 会污染上下文并导致模型请求失败。

**禁止主动结束 OfficeCLI 进程。** 不要执行 `taskkill /F /IM officecli.exe`。如果脚本提示文档锁定，按脚本提示让老师关闭 Word 后重试。

**禁止在老师使用过程中修改 skill 源码。** 不要用 `edit_file` 修改 `skills/political-meeting/scripts/*.py`，不要删除 `__pycache__`，不要运行临时 `python -c` 调试脚本。线上失败只能反馈错误或等待开发者发布新版 skill。

所有文件操作交给 `insert_material.py` 和 `check_status.py`。脚本内部会用 OfficeCLI 处理，包括 `.doc` 自动转 `.docx` 的尝试。

## 流程

```
老师发文件
  → 解析文件名提取 周次+类型
  → 能确定周次和类型：立即执行脚本归入总文档
  → 不能确定图片类型：追问"这是签到表还是会议照片？"
  → 执行后立刻查询该周状态
  → 回复老师：已归入 + 还缺哪些材料
```

## 收到可识别材料后（必须执行）

**先确认路径：** 读 `skills/political-meeting/config.json`。如果 `总表路径` 为空或文件不存在，先问老师，不要继续执行插入：

> 老师，我还不知道政治学习总文档在哪。请发送总文档，或告诉我总文档的完整路径，我会先记录下来再归档本次材料。

**路径有值 → 按文件逐个执行：**
```bash
python skills/political-meeting/scripts/insert_material.py --week-title "2025学年第二学期第九、十周政治学习" --type "学习内容" --file "文件路径"
```

连续周次（9+10）合并为"第九、十周"。

**全部执行完 → 汇总：**
```bash
python skills/political-meeting/scripts/check_status.py --week-title "第九、十周政治学习"
```

输出发给老师。**不要自己编"已完成/已归档"，必须等脚本输出。**

## 脚本失败时

只允许三种处理：

1. 参数明显写错：修正 `--docx`、`--week-title`、`--type`、`--file` 后重跑同一个脚本。
2. 脚本提示 Word 占用：请老师关闭总文档，然后重跑同一个脚本。
3. 其他异常：把脚本错误原文简要反馈给老师，停止操作。

不要为了“继续试试”改用 PowerShell、Python 临时脚本、docx 解压、XML 读取、手写 OfficeCLI 命令或直接读取 Word 内部文件。

## 固定插入顺序（严禁打乱）

每一周或合并周次都必须保持以下顺序：

1. `会议记录`：只写这四个字
2. `会议签到表`：签到表图片放在这个标题后、周标题前
3. 周标题：如 `2025学年第二学期第九、十周政治学习`
4. 简要介绍：只写学校、学期、周次和学习材料标题列表；不要写正文摘要，不要摘录学习内容段落。格式为 `广州市番禺区番广附万博学校2025学年第二学期第九、十周政治学习：学习材料标题1、学习材料标题2`
5. `会议照片：`：即使会议照片尚未上传，也保留这一行作为占位
6. 学习内容：学习材料正文追加在会议照片区域之后

材料异步到达时也按这个顺序归位：学习内容先到，先创建标题、简要介绍和 `会议照片：` 占位；会议照片或签到表后到，插回对应位置。

如果脚本提示总文档正被 Word 或其他程序打开，先请老师关闭总文档，再继续归档或查询状态。

如果老师说“已经被部分归档过，继续补充，不要破坏原文档”，继续用同一个 `--week-title` 和 `--type "学习内容"` 重跑 `insert_material.py`。脚本会把新学习内容追加到该周末尾，不要询问替换，也不要手工编辑 Word。

## 文件名解析

| 文件名含 | → 周次 | → 类型 |
|---------|--------|--------|
| `第X周` | X | — |
| `学习内容`、`政治学习` | — | 学习内容 |
| `签到表`、`签到` | — | 签到表 |
| `照片`、`会议照片` | — | 会议照片 |
| `.doc`、`.docx` | — | 大概率学习内容 |
| `.jpg`、`.png` | — | 需追问（签到表/照片？） |

## 常见对话处理

**老师一次发两个文档：**
- 如果文件名分别含第9周、第10周，且老师说"把这两周的学习材料归入总文档"，合并为 `2025学年第二学期第九、十周政治学习`
- 两个文档都按 `学习内容` 执行 `insert_material.py`
- 最后执行 `check_status.py --week-title "第九、十周政治学习"`
- 回复示例：`已将第九、十周学习内容归入总文档。当前还缺：签到表、会议照片。`

**老师只发第九周学习内容：**
- 如果能判断后续会与第十周合并，但第十周材料还没到，先按第九周处理；老师之后明确两周合并时，再使用合并标题处理后续材料
- 完成后必须告诉老师还缺哪些材料

## 查询

老师问"还差什么"/"进度" → 直接执行：
```bash
python skills/political-meeting/scripts/check_status.py --week-title "XX周政治学习"
```
